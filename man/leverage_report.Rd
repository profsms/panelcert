% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R, R/leverage.R
\name{leverage_report.fixest}
\alias{leverage_report.fixest}
\alias{leverage_report.plm}
\alias{leverage_report.lm}
\alias{leverage_report}
\alias{leverage_report.default}
\title{Module A diagnostic: variance-estimator adequacy under FE saturation}
\usage{
\method{leverage_report}{fixest}(object, ...)

\method{leverage_report}{plm}(object, ...)

\method{leverage_report}{lm}(object, x, unit, time, ...)

leverage_report(object, ...)

\method{leverage_report}{default}(object, x, unit, time, alpha = 0.05, delta = 0.05, ...)
}
\arguments{
\item{object}{outcome vector (default method), or a fitted \code{fixest} /
\code{plm} / \code{lm} model with one regressor and two-way fixed effects}

\item{...}{passed between methods}

\item{x}{regressor of interest; for the \code{lm} method, its NAME in the
model frame}

\item{unit}{unit identifiers (any type); for the \code{lm} method, the name
of the unit variable in the model frame}

\item{time}{period identifiers (any type); for the \code{lm} method, the
name of the time variable in the model frame}

\item{alpha}{nominal test level}

\item{delta}{size-distortion tolerance}
}
\value{
an object of class \code{AdequacyReport}
}
\description{
Reproduces the user's FE regression of `y` on `x` with unit and time fixed
effects via Frisch-Waugh (never re-specified), then reports the naive / CJN /
HC0-HC3 variance hierarchy, the leverage diagnostics, and whether the
variance-estimator choice materially changes inference at tolerance `delta`.
}
\details{
Verdict: FLAGGED when the asymptotic HC0/naive over-rejection exceeds
`alpha + delta` at this design's saturation `rho`, or when significance at
level `alpha` flips across the estimators on this data.
}
\examples{
n <- 200; unit <- rep(1:20, each = 10); time <- rep(1:10, times = 20)
x <- rnorm(n); y <- 0.5 * x + rnorm(n)
leverage_report(y, x, unit, time)
}
\references{
Halkiewicz, S. M. S. Variance estimation for saturated
  fixed-effect specifications (Paper A).
}
